{
  "name": "imovel",
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "deploy": "AWS_PROFILE=dev sst deploy --stage prod",
    "dev": "AWS_PROFILE=dev sst dev"
  },
  "devDependencies": {
    "@tsconfig/node22": "~22.0.2",
    "typescript": "~5.8.3"
  },
  "dependencies": {
    "sst": "~3.17.0"
  }
}
