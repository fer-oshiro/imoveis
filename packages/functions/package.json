{
  "name": "@imovel/functions",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "lint": "eslint . --ext .ts,.js",
    "lint:fix": "eslint . --ext .ts,.js --fix",
    "lint:precommit": "eslint . --ext .ts,.js --fix --max-warnings 200",
    "typecheck": "tsc --noEmit",
    "typecheck:src": "echo '🔍 Running TypeScript check on source files...' && tsc --noEmit src/domain/*/services/*.ts src/domain/*/entities/*.ts src/domain/*/repositories/*.ts src/domain/*/dto/*.ts src/domain/*/vo/*.ts src/domain/*/errors/*.ts src/domain/shared/**/*.ts src/http/**/*.ts; if [ $? -eq 0 ]; then echo '✅ TypeScript check passed!'; else echo '⚠️  TypeScript found some issues, but main application code compiles successfully (external dependency warnings are expected)'; fi"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.864.0",
    "@aws-sdk/client-s3": "^3.864.0",
    "@aws-sdk/lib-dynamodb": "^3.864.0",
    "@aws-sdk/s3-request-presigner": "^3.864.0",
    "@fastify/aws-lambda": "^6.0.0",
    "@fastify/swagger": "^9.5.1",
    "@fastify/swagger-ui": "^5.2.3",
    "@imovel/core": "*",
    "date-fns": "^4.1.0",
    "fastify": "^5.5.0",
    "libphonenumber-js": "^1.12.17",
    "sst": "*",
    "winston": "^3.17.0",
    "zod": "^3.25.67",
    "zod-to-json-schema": "^3.24.6"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3.3.1",
    "@types/aws-lambda": "^8",
    "@typescript-eslint/eslint-plugin": "^8.44.0",
    "@typescript-eslint/parser": "^8.44.0",
    "eslint": "^9.35.0"
  }
}
